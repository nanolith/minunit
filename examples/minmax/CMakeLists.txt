AUX_SOURCE_DIRECTORY(src MINMAX_SOURCES)
AUX_SOURCE_DIRECTORY(test TESTMINMAX_SOURCES)

ADD_EXECUTABLE(testminmax ${MINMAX_SOURCES} ${TESTMINMAX_SOURCES})

TARGET_COMPILE_OPTIONS(testminmax PRIVATE --coverage -Wall -Werror)
TARGET_LINK_LIBRARIES(testminmax PRIVATE --coverage minunit)
